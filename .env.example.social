# Social Posting MVP - Environment Variables
# Copy to .env and fill in your values

# ============================================================================
# Database (Supabase)
# ============================================================================
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================================
# Token Encryption
# ============================================================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This key encrypts OAuth tokens at rest in the database
# CRITICAL: Keep this secret and backed up. Losing it means losing access to stored tokens.
SOCIAL_TOKEN_ENCRYPTION_KEY=your-64-character-hex-key-here

# ============================================================================
# Instagram (Meta/Facebook App)
# ============================================================================
# Create app at: https://developers.facebook.com/apps/
# Required permissions: instagram_basic, instagram_content_publish
# Redirect URI: https://your-app.netlify.app/.netlify/functions/social-accounts-callback
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret

# ============================================================================
# YouTube (Google Cloud Console)
# ============================================================================
# Create project at: https://console.cloud.google.com/
# Enable YouTube Data API v3
# Create OAuth 2.0 credentials
# Required scopes: youtube.upload, youtube.readonly
# Redirect URI: https://your-app.netlify.app/.netlify/functions/social-accounts-callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================================================
# Scheduled Functions
# ============================================================================
# Secret key for protecting scheduled function endpoints
# Generate with: openssl rand -base64 32
WEBHOOK_SECRET=your-webhook-secret

# ============================================================================
# App Configuration
# ============================================================================
# Your app's public URL (used for OAuth redirects)
URL=https://your-app.netlify.app

# Feature flag for social posting
SOCIAL_POSTING_ENABLED=true

# ============================================================================
# Optional: Monitoring & Analytics
# ============================================================================
# Add your monitoring service credentials here if needed
# SENTRY_DSN=
# LOGTAIL_TOKEN=
